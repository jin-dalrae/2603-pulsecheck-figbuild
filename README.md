
  # pulse

  This is a code bundle for pulse. The original project is available at https://www.figma.com/design/XgSdCNZEsdWInJUhSsfjuy/pulse.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  